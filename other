/*
#include <stdarg.h>
#include <stdio.h>

int _printf(const char *format, ...) {
    va_list args;
    int len = 0;
    char *p;

    va_start(args, format);

for (p = format; *p; p++)
{
if (*p != '%')
{
_putchar(*p);
len++;
continue;
}
format_specifiers(*++p; args; &len);
}

    va_end(args);
    return len;
}
*/

/*
format_specifiers(char ch, va_list args, int *len)
{
switch....
}
*/
